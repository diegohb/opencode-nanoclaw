# Branch & Fork Maintenance

This document was merged into the authoritative fork policy in `FORK-MAPPING.md`.

Use `FORK-MAPPING.md` for all branch roles, sync workflow, replay guidance, conflict hotspots, and operator rules.

If maintenance guidance is updated, update `FORK-MAPPING.md` only.
